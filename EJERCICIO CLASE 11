#   Crear un módulo llamado persistencia_picke similar a persistencia_json
#   Completr el ejercicio de clase 10 escribiendo y leyendo la lista de objetos de archivo mediante las funciones store y retrieve desarrolladas anteriormemnte en el módulo
#   persistencia_pickle
#   poner la definicion de la clase en un modulo aparte que se llame "car_class"
#   modificar funcion retrieve para que use "exceptions" al abrir el archivo con los datos para controlar cualquier error en la apertura del archivo

